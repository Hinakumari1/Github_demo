name: Greet Me
on:
  workflow_dispatch:
    inputs:
        name:
            description: 'Your name'
            required: true
            default: 'Github User'
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, ${{ github.event.inputs.name }}! 👋"